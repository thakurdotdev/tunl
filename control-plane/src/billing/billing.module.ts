import { Module } from '@nestjs/common';

// Phase 3: Stripe integration. Build the folder + a no-op module now so the
// shape exists (plan section 2.5) — no controllers/services yet.
@Module({})
export class BillingModule {}
